# ──────────────────────────────────────────────────────────────────
# To-Do List Bot Gen 2 — Requirements
# Pin minor versions for reproducible builds
# ──────────────────────────────────────────────────────────────────

# Discord
discord.py>=2.4.0

# Environment
python-dotenv>=1.0.0

# Timezone support
pytz>=2024.1

# Async utilities
aiofiles>=23.2.1
aiohttp>=3.9.0

# Async web keep-alive server (replaces Flask — fully non-blocking)
# aiohttp is already a dependency above, no extra package needed

# (Optional) Faster event loop — Linux/macOS only, silently ignored on Windows
uvloop>=0.19.0; sys_platform != "win32"
